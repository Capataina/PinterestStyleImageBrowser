export type ImageData = {
  path: string;
  name: string;
  tags: string[];
  id: string;
};

export type ImageItem = {
  id: string;
  url: string;
  width: number;
  height: number;
  name: string;
  tags: string[];
};
